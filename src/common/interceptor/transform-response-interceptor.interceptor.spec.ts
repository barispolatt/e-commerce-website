import { TransformResponseInterceptorInterceptor } from './transform-response-interceptor.interceptor';

describe('TransformResponseInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformResponseInterceptorInterceptor()).toBeDefined();
  });
});
